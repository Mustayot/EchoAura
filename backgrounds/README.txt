把你的背景图片（jpg / png）放进这个文件夹，程序就会自动用它做界面背景。

使用方法：
1. 把图片文件（如 mybg.jpg）放进本文件夹
2. 打开 config.py，设置：
       BACKGROUND_IMAGE = "mybg.jpg"   # 填图片文件名；留空=自动用第一张
3. 保存后重新运行程序

提示：
- 建议用 1920x1080 或更大分辨率的图，会自动铺满窗口
- 一张图都没放 = 用默认的白粉渐变背景
- 程序(exe)版：把这个文件夹放在 exe 旁边即可
